# Findings — drift gate / advisor / ledger / role / #121 / billing-keys audit

Verify-first; evidence per item; explicit handback to Lauren. Claude ran **no**
SQL/migrations against prod (Lauren-runs model). Prod reads were limited to the
read-only security advisor and CI artifacts.

---

## A — Drift-gate trip cause (pulled, not inferred)
**Evidence:** drift-gate run `28061021442` on `main` — only the **`policies`**
dimension DRIFTs (live 48 / ref 46); every other dimension MATCHES, incl.
`view_body` (`5fc8179a == 5fc8179a`). The `+2` are
`deny_all_non_service` on `notification_dead_letter` and `wles_v1_watermark`.
**The evidence-note hypothesis (view/ledger) was wrong** — good that A required
the artifact. These two policies are **prod-only** (no committed migration
creates them; `sec5_advisor_cleanup` documents they were *intentionally* omitted
as redundant + fingerprint-breaking).
**Fix:** drop them in prod → 46 == ref == rebuild. **→ LAUREN-RUNBOOK Step 1.**

## B — SECURITY DEFINER regression on v_anchor_verification
**Evidence:** `get_advisors(security)` returns one ERROR — `security_definer_view`
on `public.v_anchor_verification` (external-facing, lint 0010). Root cause: my
`20260623160000_wles_a1` recreated the view with a bare `CREATE OR REPLACE`,
resetting the `security_invoker` that `m3a` set.
**Fix (repo, PR #187):** re-assert migration `20260623190000` +
`tests/substrate/security-invoker-views.test.ts` guard (models migration order;
fails if any invoker view ends DEFINER; 4/4 green). **Fix (prod):**
`ALTER VIEW … SET (security_invoker = true)` **→ LAUREN-RUNBOOK Step 2.**

## C — Sweep of all CREATE OR REPLACE VIEW for the same bug
**Evidence:** the three public views meant to be invoker —
`v_anchor_verification` (regressed), `v_security_advisor_sweep` (inline
`with (security_invoker = true)`, OK), `v_shift_commit_orphans` (inline
`with (security_invoker = on)`, OK). **Only the anchor view regressed.** The PR
#187 guard now covers all three (it caught the `on` vs `true` spelling).

## D — Migration-ledger mismatch
**Evidence (from the brief; not re-queried — no prod SQL):** committed filenames
reach `20260623190000`; prod `schema_migrations` recorded e.g. `wles_a1` as
`20260623025032` and tops ~`20260623032320`. Schema *effects* are applied
(drift gate confirms catalogs match); only the ledger version strings differ.
**This does NOT trip the drift gate** (which reads live catalogs, not the
ledger). **Fix:** diagnose-then-reconcile — Lauren dumps the ledger (3a), then
aligns version strings. **→ LAUREN-RUNBOOK Step 3.** (Needs the dump to produce
exact UPDATE/INSERTs.)

## E — Drift-gate role (PR #51 `drift-gate-role.sql`)
**Evidence:** the role grants `CONNECT` + `USAGE` only, **no SELECT** on any
table/view. Every drift-gate query reads system catalogs + `pg_get_*def`
functions, which reconstruct from catalog OIDs **without** an ACL check on the
target — so no SELECT is needed, including `view_body`/`pg_get_viewdef`. The
`drift-gate.mjs` comment ("view_body needs SELECT on the view") is over-cautious
and is contradicted by the live gate already producing `view_body MATCH`.
**Conclusion: grants are sufficient — no correction needed.** Empirical
confirmation (`SET ROLE drift_gate_ro` + run each fingerprint query)
**→ LAUREN-RUNBOOK Step 4.**

## F — PR #121 (bare `catch {}` dropping payroll/dispute notices)
**Evidence:** `OPEN`, `MERGEABLE`, was `BEHIND` main → updated onto main; CI
re-running. Once green it is **ready to merge** (held per the no-merge-without-
go-ahead rule). The three silent `catch {}` in `sms-reply/route.ts`
(handleYesAll/handleYesCode → notifyPayrollAdmin, handleNoCode →
notifyPayrollDispute) route to `recordNotificationDeadLetter` (parity with E2/#119).

## G — Billing code vs LIVE Stripe lookup keys
**Evidence:**
- Canonical spec = `bots/15-proposal-quote/pricing-spec.ts` (ex-GST). Its amounts
  **exactly match** `src/lib/billing/pricing-v1_1.ts` and the live keys'
  amounts (enterprise base AUD 1,000; onboarding base AUD 5,000). **Amounts ✓.**
- **Key MISMATCH (real bug):** `src/app/api/stripe/checkout/route.ts` resolves
  `tier.stripe_lookup_monthly` from the legacy `src/lib/stripe/pricing.ts` TIERS,
  which emit `founding-monthly` / `standard-monthly` / `growth-monthly` /
  `scale-monthly`. The **live** keys are `flostruction_starter_monthly_v1`,
  `flostruction_growth_monthly_v1`, `flostruction_growth_onboarding_v1`,
  `flostruction_enterprise_base_v1`, `flostruction_enterprise_worker_v1`,
  `flostruction_enterprise_onboarding_base_v1`. The old Standard/Founding products
  are **inactive** — so a checkout today would request a non-existent lookup_key
  and fail.
- **Partial code fix prepared:** the authoritative live-key mapping is added to
  `pricing-v1_1.ts` (`stripeLookupKeys` per tier) + a reconciliation test (keys ==
  live, amounts == spec).
- **Remaining (handback):** wiring `checkout` + `webhook-handlers` off legacy
  `pricing.ts` onto `pricing-v1_1.ts` + the live keys is the v1.1 step-3/4 cutover
  — a larger, separately-verified PR. Flagged, not done speculatively.

---

## Handback to Lauren (explicit)
1. **Run LAUREN-RUNBOOK** Steps 1–4 (drop policies; ALTER VIEW invoker; ledger
   dump→reconcile; SET ROLE self-check). Steps 1+2 each close a DoD item.
2. **After Step 1:** re-run the drift gate → expect GREEN.
3. **After Step 2:** re-check the advisor → expect ZERO error-level.
4. **Send back the ledger dump** (Step 3a) so the exact reconcile SQL can be cut.
5. **Go-ahead to merge** PR #187 (invoker guard) and PR #121 (dead-letter) once
   their CI is green — neither merged without your word.
6. **Decide** whether to schedule the **checkout/webhook cutover** to the live
   `flostruction_*_v1` keys (Task G remaining) as its own PR.
7. Owner toggles unchanged: leaked-password protection (the lone WARN advisor).

## DoD status (honest)
- [prepared] invoker rebuild guard + re-assert migration (PR #187) — **verified**
  (guard 4/4 green); prod ALTER + advisor=0 **pending Lauren** (Step 2).
- [prepared] drift gate green — **pending Lauren Step 1** (drop policies); cause
  captured.
- [done] PR #121 updated; CI re-running → ready to merge on green.
- [done] drift-gate role verified sufficient; empirical SET-ROLE check handed over.
- [partial] billing: amounts reconciled ✓; live-key map added + tested; checkout
  cutover handed back.
