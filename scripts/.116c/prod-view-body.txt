public.v_anchor_verification ::  SELECT id,\n    scope_text,\n    expected_fingerprint,\n    expected_count,\n    bound_at,\n        CASE id\n            WHEN 'FROZEN_ANCHOR_V0'::text THEN ( SELECT md5(string_agg((shift_events.id::text || ':'::text) || shift_events.event_hash, '|'::text ORDER BY shift_events.created_at, shift_events.id)) AS md5\n               FROM shift_events\n              WHERE shift_events.spec_version = '0'::text AND shift_events.created_at < '2026-06-04 02:56:50+00'::timestamp with time zone)\n            WHEN 'FROZEN_ANCHOR_V1'::text THEN ( SELECT md5(string_agg((shift_events.id::text || ':'::text) || shift_events.event_hash, '|'::text ORDER BY shift_events.created_at, shift_events.id)) AS md5\n               FROM shift_events\n              WHERE shift_events.spec_version = '1.0'::text AND shift_events.wles_event IS NOT NULL AND shift_events.created_at < '2026-06-19 00:00:00+00'::timestamp with time zone)\n            ELSE NULL::text\n        END AS actual_fingerprint,\n        CASE id\n            WHEN 'FROZEN_ANCHOR_V0'::text THEN ( SELECT count(*)::integer AS count\n               FROM shift_events\n              WHERE shift_events.spec_version = '0'::text AND shift_events.created_at < '2026-06-04 02:56:50+00'::timestamp with time zone)\n            WHEN 'FROZEN_ANCHOR_V1'::text THEN ( SELECT count(*)::integer AS count\n               FROM shift_events\n              WHERE shift_events.spec_version = '1.0'::text AND shift_events.wles_event IS NOT NULL AND shift_events.created_at < '2026-06-19 00:00:00+00'::timestamp with time zone)\n            ELSE NULL::integer\n        END AS actual_count,\n        CASE id\n            WHEN 'FROZEN_ANCHOR_V0'::text THEN expected_fingerprint = (( SELECT md5(string_agg((shift_events.id::text || ':'::text) || shift_events.event_hash, '|'::text ORDER BY shift_events.created_at, shift_events.id)) AS md5\n               FROM shift_events\n              WHERE shift_events.spec_version = '0'::text AND shift_events.created_at < '2026-06-04 02:56:50+00'::timestamp with time zone)) AND expected_count = (( SELECT count(*) AS count\n               FROM shift_events\n              WHERE shift_events.spec_version = '0'::text AND shift_events.created_at < '2026-06-04 02:56:50+00'::timestamp with time zone))\n            WHEN 'FROZEN_ANCHOR_V1'::text THEN expected_fingerprint = (( SELECT md5(string_agg((shift_events.id::text || ':'::text) || shift_events.event_hash, '|'::text ORDER BY shift_events.created_at, shift_events.id)) AS md5\n               FROM shift_events\n              WHERE shift_events.spec_version = '1.0'::text AND shift_events.wles_event IS NOT NULL AND shift_events.created_at < '2026-06-19 00:00:00+00'::timestamp with time zone)) AND expected_count = (( SELECT count(*) AS count\n               FROM shift_events\n              WHERE shift_events.spec_version = '1.0'::text AND shift_events.wles_event IS NOT NULL AND shift_events.created_at < '2026-06-19 00:00:00+00'::timestamp with time zone))\n            ELSE NULL::boolean\n        END AS matches,\n    now() AS recomputed_at\n   FROM substrate_anchors a;
